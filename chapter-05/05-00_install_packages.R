# To install on AWS EC2 GPU instances running Amazon Linux AMI
# with NVIDIA GRID GPU Driver,
# first run the following in a terminal window:
# export CUDA_HOME=/opt/nvidia/cuda
# export LD_LIBRARY_PATH=/opt/nvidia/cuda/lib64:$LD_LIBRARY_PATH

install.packages("gputools")
